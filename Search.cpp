#include "Search.h"
using namespace std;

vector<size_t> searchFor(const string& pattern,
                         const string& text,
                         const SuffixArray& suffixArr) {
  /* TODO: Implement this! */
  (void) pattern;
  (void) text;
  (void) suffixArr;
  return {};
}
